import path from "path";

export const PATH_DELIMITER = `${path.delimiter}${path.delimiter}`;

export const PUSH_RETRY_WAIT = 3000;
export const PUSH_RETRY_LIMIT = 3;

export const CONCURRENCY_TABLES = 2;
export const CONCURRENCY_RECORDS = 5;
export const CONCURRENCY_FILES = 10;
export const CONCURRENCY_PUSH = 3;
export const CONCURRENCY_BUILD = 5;
